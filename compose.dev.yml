services:
  # The "Master" service that defines the build
  web:
    image: bahrain-cpr-extract:latest # Name the image so the worker can reuse it
    environment:
      - REDIS_HOST=redis
    ports:
      - "8000:8000"
    depends_on:
      - redis
    networks:
      - cpr_net

  # The worker reuses the image built by 'web'
  worker:
    image: bahrain-cpr-extract:latest
    # depends_on:
    #   web:
    #     condition: service_healthy # Optional: wait for web to be ready
    #   redis:
    #     condition: service_started
    command: ["python", "start_worker.py"]
    networks:
      - cpr_net
    mem_limit: 8g
    memswap_limit: 8g
    cpus: 2.0

  redis:
    image: redis:alpine
    networks:
      - cpr_net

networks:
  cpr_net:
    driver: bridge
